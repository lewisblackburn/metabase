mutation UpsertNotifications($objects: [notifications_insert_input!]!) {
    insert_notifications(objects: $objects, on_conflict: { constraint: notifications_pkey, update_columns: [] }) {
        affected_rows
    }
}
