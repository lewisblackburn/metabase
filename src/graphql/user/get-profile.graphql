query GetProfile($id: uuid!) {
    user(id: $id) {
        id
        displayName
        email
        lastSeen
        createdAt
        avatarUrl
        emailVerified
        disabled
        locale
    }
}
