query GetFollowees($id: uuid!) {
    user(id: $id) {
        followees {
            followee {
                id
                displayName
                email
            }
        }
        followees_aggregate {
            aggregate {
                count
            }
        }
    }
}
