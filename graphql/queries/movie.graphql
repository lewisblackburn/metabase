query Movie($id: uuid!) {
    movies_by_pk(id: $id) {
        id
        poster_id
        backdrop_id
        title
        overview
        certification
        release_date
        runtime
        genres {
            genre {
                name
            }
        }
    }
}
