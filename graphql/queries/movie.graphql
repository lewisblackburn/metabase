query Movie($id: uuid!) {
    movies_by_pk(id: $id) {
        id
        poster_id
        backdrop_id
        title
        tagline
        overview
        certification
        release_date
        runtime
        vote_average
        genres {
            genre {
                id
                name
            }
        }
    }
}
